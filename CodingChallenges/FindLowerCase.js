function detectWord(str) {
    let word = "";
    for (i = 0; i <str.length; i++) {
        if (str[i].toLowerCase() = str[i]) {
            letter = str[i];
            word += letter;
        } 
    }
    return word;
}